class CreateComments < ActiveRecord::Migration
  def change
    create_table :comments do |t|
      t.text :comment
      t.references :user
      t.references :slot_assess

      t.timestamps
    end
    add_index :comments, :user_id
    add_index :comments, :slot_assess_id
  end
end
